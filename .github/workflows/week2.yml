name: week 1

on:
 push:
 workflow_dispatch:

jobs:
      hello:
            runs-on: ubuntu-latest
            steps:
            - name: today date
              run: date
            - name: show present dir  
              run: pwd

